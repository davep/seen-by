[project]
name = "seen-by"
version = "0.1.0"
description = "Build system for seen-by.davep.dev"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "blogmore>=2.10.0",
    "pillow>=11.0.0",
    "python-slugify>=8.0.0",
]
